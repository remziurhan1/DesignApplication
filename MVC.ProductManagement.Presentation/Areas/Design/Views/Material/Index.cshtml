 @model List<MVC.ProductManagement.Presentation.Areas.Design.Models.MaterialVMs.MaterialListVm>

@{
    ViewData["Title"] = "Material List";
}

<h2>Materials</h2>

<p>
    <a asp-area="Design" asp-controller="Material" asp-action="Create" class="btn btn-primary">+ New Material</a>
</p>

<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Name</th>
            <th>Material Number</th>
            <th>Density</th>
            <th>Notes</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.MaterialNumber</td>
                <td>@item.Density</td>
                <td>@item.Notes</td>
                <td>
                    <a asp-area="Design" asp-controller="Material" asp-action="Details" asp-route-id="@item.Id" class="btn btn-info btn-sm">Details</a>
                    <a asp-area="Design" asp-controller="Material" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning btn-sm">Edit</a>
                    <a asp-area="Design" asp-controller="Material" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm">Delete</a>
                    <!-- ✅ Yeni Forms butonu -->
                    <a asp-area="Design" asp-controller="MaterialForm" asp-action="Index" asp-route-materialId="@item.Id"
                       class="btn btn-secondary btn-sm">Forms</a>

                </td>
            </tr>
        }
    </tbody>
</table>
